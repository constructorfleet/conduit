# Token-Based Operator Access

The first operator UI will authenticate to Conduit's service API with a management bearer token, or use explicit anonymous mode when the server is configured to allow it. Conduit does not yet have user accounts or browser sessions, and adding them now would expand backend scope before the operator console needs that complexity.
