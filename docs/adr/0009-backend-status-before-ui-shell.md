# Backend Status Before UI Shell

The main operator UI should wait for backend snapshot and status surfaces rather than deriving core state from partial APIs. Runtime status, satellite status, provider status, pipeline and component health projections, and typed API/event contracts are prerequisites for a UI that can be event-driven without guessing.
