; A Wyoming handler is asynchronous, so the tests are too. `strict` mode keeps
; the marker explicit: an async test that silently never ran would be a test
; suite that proves nothing.
[pytest]
asyncio_mode = strict
