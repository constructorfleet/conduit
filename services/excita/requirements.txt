fastapi>=0.110
uvicorn[standard]>=0.24
httpx==0.28.1
pydantic==2.13.4
psycopg[binary]>=3.2.0
python-multipart>=0.0.9
# Wake-word runtime. openwakeword ships as a small pure-Python package that
# expects a tflite / onnxruntime backend at import time; we pin the ONNX
# path because it's the same runtime the Rust conduit-wake crate uses, so
# the two agree on which artifacts to trust.
openwakeword>=0.6.0
onnxruntime>=1.16
numpy>=1.24
# microWakeWord offline scoring (spec 0013). Hard dep in the image — one
# image, one behavior, no per-engine variant. Marker only skips it on
# dev hosts where tflite-runtime publishes no wheels (macOS); there the
# adapter raises honestly instead of scoring.
tflite-runtime>=2.14; sys_platform == "linux"
# nanoWakeWord live detection + scoring (spec 0013). Pure Python on top of
# onnxruntime; its preprocessing ONNX files download on first model load.
nanowakeword>=0.1
