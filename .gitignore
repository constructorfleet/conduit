/target
**/*.rs.bk
# Coverage report, written to the repo root by the `coverage` CI job.
/cobertura.xml
.DS_Store
.env
__pycache__/
.venv
/.cargo/registry

# ESPHome build output and credentials (Wi-Fi password, API encryption key).
.esphome/
secrets.yaml
