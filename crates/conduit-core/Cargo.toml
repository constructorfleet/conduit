[package]
name = "conduit-core"
description = "Core identifiers, events, event bus, and pipeline graph model for Conduit."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
chrono.workspace = true
rubato.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
tokio.workspace = true
tokio-stream.workspace = true
tracing.workspace = true
uuid.workspace = true

[features]
# Pipeline graph fixtures for tests in this workspace. Off by default so no
# release build carries fixture shapes nothing serves.
testing = []

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "sync", "time"] }
futures-util.workspace = true

[lints]
workspace = true
