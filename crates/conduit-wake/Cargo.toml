[package]
name = "conduit-wake"
description = "In-process wake word detection for Conduit, scoring openWakeWord models with no service to run."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["onnx"]
# Scoring models in process. Off, the detector still exists and refuses to
# load with a message naming this feature, so a build without an inference
# runtime says so rather than registering a detector that hears nothing.
onnx = ["dep:tract-onnx"]

[dependencies]
conduit-core.workspace = true
conduit-provider.workspace = true

async-trait.workspace = true
futures-util.workspace = true
tokio = { workspace = true, features = ["rt-multi-thread", "sync"] }
tokio-stream.workspace = true
tracing.workspace = true
tract-onnx = { version = "0.23", optional = true }

[dev-dependencies]
bytes.workspace = true
futures-util.workspace = true
hound = "3"
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "time"] }

[lints]
workspace = true
